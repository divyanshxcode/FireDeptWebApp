interface Proxy {
    host: string;
    port: number;
    protocol: string;
    auth?: {
        username: string;
        password: string;
    };
}
declare function parse(input: string): Proxy[];
export = parse;
